import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const RegContainer = styled(Box, { name: 'regcontainer' })({
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
});
